DATABASE_URL=postgresql+psycopg://awaken:awaken@db:5432/awaken
REDIS_URL=redis://redis:6379/0

# Optional — when unset, dialogue/belief calls return deterministic stubs.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional — HydraDB. When unset, an in-memory fallback is used.
HYDRA_API_KEY=
HYDRA_PROJECT_ID=
